name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
    
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          EXT=""
          if [ "$GOOS" = "windows" ]; then
            EXT=".exe"
          fi
          
          go build -ldflags "-s -w" -o sopa-$GOOS-$GOARCH$EXT ./cmd/sopa
          
          if [ "$GOOS" = "windows" ]; then
            zip sopa-$GOOS-$GOARCH.zip sopa-$GOOS-$GOARCH$EXT
          else
            tar czf sopa-$GOOS-$GOARCH.tar.gz sopa-$GOOS-$GOARCH$EXT
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: sopa-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            *.tar.gz
            *.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: sopa-*
          merge-multiple: true

      - name: Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create ${{ github.ref_name }} \
            *.tar.gz \
            *.zip \
            --title "${{ github.ref_name }}" \
            --generate-notes \
            --repo ${{ github.repository }}
